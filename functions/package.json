{
  "name": "caim-functions",
  "version": "1.0.0",
  "private": true,
  "description": "CAIM SaaS: githubDeployProxy (deploy na conta do Owner) + gitCorsProxy (push via isomorphic-git).",
  "main": "src/index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "seed:admin": "node src/seed-admin.js"
  },
  "dependencies": {
    "@google-cloud/secret-manager": "^5.0.0",
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^5.0.0",
    "octokit": "^3.1.0"
  }
}
